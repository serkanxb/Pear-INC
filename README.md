# Pear INC — Data Exploration Notebook

This repository contains a **Jupyter Notebook** (`pear inc.ipynb`) that performs data loading, basic inspection, and exploratory analysis on a dataset using **Pandas**, **NumPy**, and visualization libraries like **Matplotlib** and **Seaborn**.

---

## Overview

The notebook shows how to:

- Load a dataset from a CSV file using Pandas  
- Display the first rows of the dataset to verify successful loading  
- Inspect dataset properties such as shape and basic statistics  
- Use visualization tools for exploratory data analysis

---

## Features Covered

The notebook includes:

- Reading a CSV file into a DataFrame  
- Displaying data using `.head()`  
- Using `.describe()` to view summary statistics  
- Visualizing distribution and correlations with Matplotlib and Seaborn  
- Handling missing values and basic dataset inspection commands

---

## Requirements

To run this notebook, you need the following Python packages installed:

```bash
pip install pandas numpy matplotlib seaborn
